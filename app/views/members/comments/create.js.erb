$('#comment_content').val('');
$("#comments").html("<%= escape_javascript(render partial: 'members/posts/comment', collection: @post.comments) %>")
$('.comment-count').html(<%= @post.comments.count %>);
