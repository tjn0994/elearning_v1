$('#list-question-search')
  .html('<%= j render(@questions, course: @course, lesson: @lesson, questions: @questions) %>');
$('#load-page-question').html('<%= paginate @questions %>');
$('#not-exist-question').html('<%= "Dữ liệu không tìm thấy" if @search.result.blank? %>');
